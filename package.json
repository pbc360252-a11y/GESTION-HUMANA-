{
  "name": "hrm-coraza",
  "version": "1.0.0",
  "description": "Sistema de Gestion Humana - Coraza Seguridad CTA",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "node backend/src/index.js",
    "build": "npm install && npm install --prefix backend --include=dev && npm install --prefix frontend && npx prisma generate --schema=backend/prisma/schema.prisma && npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
